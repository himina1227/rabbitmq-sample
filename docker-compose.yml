version: '3'

services:
  mariadb:
    container_name: "mariadb"
    build:
      context: .
    restart: always
    ports:
      - 3306:3306
    volumes:
      - ./db/conf.d:/etc/mysql/conf.d
      - ./db/data:/var/lib/mysql
    environment:
      - TZ="Asia/Seoul"
    networks:
      - hihinakrock
networks:
  hihinakrock:
    external: true
